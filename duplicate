#include<stdio.h>
int main()
{
	int a[]={1,2,2,2,3,4};
	int i,j;
	int count=(sizeof(a)/sizeof(a[0]));
	for( i=0;i<count;i++)
	{
		for( j=i+1;j<count;j++)
		{
			if(a[i]=a[j])
			{
				printf("the duplicate element is: %d",a[i]);
			}
		}
	}
}
